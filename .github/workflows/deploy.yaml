name: Deploy node.js app to my server

on:
  push:
    branches: 
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy App
        run: |
          cd /home/muse/Tech_B
          git pull
          docker compose down
          docker compose up -d --build